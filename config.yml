# CubeX Bot v2 Configuration

# Server connection
server:
  host: "frp-day.com"
  port: 33726
  version: "1.20"
  viewDistance: "tiny"
  brand: "SteveBot"

# LLM API for quiz answering
llm:
  endpoint: "https://api.furry.vg/v1/chat/completions"
  model: "THUDM/GLM-4-9B-0414"
  apiKey: "sk-your-api-key"
  # Optional: custom headers
  headers: {}

# Bot owner (PM with # prefix triggers echo)
owner: "YourName"

# Auto eat
autoeat:
  enabled: true

# Quiz auto-answering
quiz:
  enabled: true
  minDelay: 3000        # Minimum delay before sending answer (ms)
  questionBank: "questionBank.json"  # Question bank file path
